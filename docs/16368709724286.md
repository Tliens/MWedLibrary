# Java 中的数据结构 字符串

- 字符串长度
```
int n = s.length();
```
- 字符串转数组
```
s.toCharArray()
```
- 字符串提取字符
```
s.charAt(i)
```
- 字符是否为数组
```
Character.isDigit(s.charAt(i))
```
- 字符转数字
```
s.charAt(i) - '0'
```
- 字符 == 
```
s.charAt(i) == '('
```
- 字符串 equals
```
"+".equals(token)
```