# Swift OC 交互

#### 1. OC 调用 Swift
- 引入Swift自动生成的OC头文件，可在build setting中配置名称，默认为SWIFT_MODULE_NAME-Swift.h
- 类必须继承NSObjct
- 成员变量、方法、extension前增加@objc，可以暴露给OC调用外界
- 方法中允许默认值,回被重新包装 xxx_WithX
- 方法中不允许可选值

#### 2. Swift 调用 OC
- 
#### 3. Swift 与 OC 相互引用问题


#### 4. 公用组件封装

